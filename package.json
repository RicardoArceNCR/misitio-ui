{
  "name": "@misitio/ui",
  "version": "0.1.0",
  "description": "Design system compartido: tokens de Figma via Style Dictionary. Los componentes se agregan en una version posterior.",
  "license": "UNLICENSED",
  "private": false,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RicardoArceNCR/misitio-ui.git"
  },
  "sideEffects": [
    "**/*.css"
  ],
  "exports": {
    "./tokens.css": "./build/tokens.css",
    "./tokens-dark.css": "./build/tokens-dark.css",
    "./theme-bridge.css": "./build/theme-bridge.css",
    "./build/*": "./build/*",
    "./package.json": "./package.json"
  },
  "files": [
    "build",
    "README.md"
  ],
  "scripts": {
    "tokens": "python3 figma-to-sd.py",
    "build": "style-dictionary build --config style-dictionary.config.js && style-dictionary build --config style-dictionary-dark.config.js && node -e \"require('fs').copyFileSync('theme-bridge.template.css', 'build/theme-bridge.css')\"",
    "test": "node scripts/check-units.mjs",
    "verify": "npm run tokens && npm run build && npm test",
    "watch": "nodemon --watch source --watch theme-bridge.template.css --ext json,css --exec npm run build"
  },
  "devDependencies": {
    "style-dictionary": "^4.0.0",
    "nodemon": "^3.0.0"
  }
}
